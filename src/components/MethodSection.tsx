export function MethodSection() {
  return <section className="method-section" id="method"><div className="method-intro"><p className="eyebrow"><span className="eyebrow-line" /> BEHIND THE BRIEFING</p><h2>Claims meet<br /><em>evidence.</em></h2><p>AI Daily keeps the public edition small and inspectable. We publish a claim only when the review panel can point to supporting evidence.</p></div><div className="review-panel"><div className="review-head"><span>THE TRUST CONTRACT</span><span>V1</span></div>{['Every claim has a source','Corrections stay visible','The list works without 3D'].map((item, index) => <div className="review-step" key={item}><strong>0{index + 1}</strong><div><b>{item}</b><p>Inspectable from every story drawer</p></div><span className="check">✓</span></div>)}</div></section>;
}
